import React from 'react';

const VideoChat = (props) => (
    <div >
        <button onClick={props.stopCall}>
                Terminar llamada
        </button>);
    </div>
);

export default VideoChat;
