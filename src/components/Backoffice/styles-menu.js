import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  menu: {
    minHeight: '100vh',
    paddingTop: '5vh',
    justifyItems: 'center',
    fontSize: '12px',
  },
  tab: {
    paddingTop: '5vh',
  },
}));

export default useStyles;
